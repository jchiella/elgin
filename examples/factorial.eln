#: Simple factorial function
#: Also a great example of recursion
proc factorial(n: i32): i32 {
    if (n == 0) {
        return 1
    } else {
        return n * factorial(n - 1)
    }
}

proc main(): i32 {
    var res = factorial(5)
    return res
}

